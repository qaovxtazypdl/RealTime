/* Global track information, should be initialized using one of init_tracka or init_trackb. */
#ifndef _TRACK_H_
#define _TRACK_H_
#include <track_node.h>

extern struct track_node g_track[];
extern struct track_node *g_sensors[];

#endif
